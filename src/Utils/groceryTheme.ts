import { tokens } from "@src/Utils/tokens";

export const groceryTheme = {
  colors: tokens.colors,
  spacing: tokens.spacing,
  radius: tokens.radius,
  typography: tokens.typography,
  shadows: tokens.shadows,
};
